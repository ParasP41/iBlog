<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include('./navbar.ejs') %>


        <div class="min-h-screen bg-gray-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">

                <!-- Header -->
                <div class="mb-12">
                    <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
                        <%= category==='all' ? 'All Posts' : category.charAt(0).toUpperCase() + category.slice(1) %>
                    </h1>
                    <p class="text-xl text-gray-600 max-w-3xl">
                        <% if (category==='all' ) { %>
                            Explore all our articles across different categories and topics.
                            <% } else { %>
                                Discover the latest insights and articles about <%= category %>.
                                    <% } %>
                    </p>
                </div>

                <!-- Filters and Stats -->
                <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8 gap-4">
                    <div class="text-gray-600">
                        Showing <%= startIndex + 1 %>-<%= Math.min(endIndex, sortedPosts.length) %> of <%=
                                    sortedPosts.length %> posts
                    </div>

                    <div class="flex items-center space-x-4">
                        <span class="text-sm text-gray-600">Sort by:</span>
                        <form method="GET" action="">
                            <select name="sortBy" onchange="this.form.submit()" class="border rounded px-3 py-2">
                                <option value="latest" <%=sortBy==='latest' ? 'selected' : '' %>>Latest</option>
                                <option value="oldest" <%=sortBy==='oldest' ? 'selected' : '' %>>Oldest</option>
                                <option value="popular" <%=sortBy==='popular' ? 'selected' : '' %>>Most Popular</option>
                            </select>
                        </form>
                    </div>
                </div>

                <!-- Posts Grid -->
                <% if (currentPosts.length> 0) { %>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
                        <% currentPosts.forEach(post=> { %>
                            <%- include('partials/BlogCard', { post }) %>
                                <% }) %>
                    </div>

                    <!-- Pagination -->
                    <% if (totalPages> 1) { %>
                        <div class="flex justify-center items-center space-x-4">
                            <form method="GET" action="">
                                <input type="hidden" name="page" value="<%= currentPage - 1 %>" />
                                <button class="border px-4 py-2 rounded" <%=currentPage===1 ? 'disabled' : ''
                                    %>>Previous</button>
                            </form>

                            <div class="flex space-x-2">
                                <% for (let i=1; i <=totalPages; i++) { %>
                                    <form method="GET" action="">
                                        <input type="hidden" name="page" value="<%= i %>" />
                                        <button
                                            class="w-10 h-10 rounded <%= currentPage === i ? 'bg-blue-600 text-white' : 'border' %>">
                                            <%= i %>
                                        </button>
                                    </form>
                                    <% } %>
                            </div>

                            <form method="GET" action="">
                                <input type="hidden" name="page" value="<%= currentPage + 1 %>" />
                                <button class="border px-4 py-2 rounded" <%=currentPage===totalPages ? 'disabled' : ''
                                    %>>Next</button>
                            </form>
                        </div>
                        <% } %>
                            <% } else { %>
                                <div class="text-center py-12">
                                    <h3 class="text-2xl font-semibold text-gray-900 mb-4">No posts found</h3>
                                    <p class="text-gray-600 mb-8">We couldn't find any posts in this category. Try
                                        browsing other categories.</p>
                                    <button onclick="window.history.back()"
                                        class="bg-blue-600 text-white px-6 py-2 rounded">Go Back</button>
                                </div>
                                <% } %>

            </div>
        </div>
        <%- include('./footer') %>



</body>

</html>