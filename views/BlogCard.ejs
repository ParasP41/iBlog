<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <article class="group bg-white rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 overflow-hidden <%#= featured ? 'md:col-span-2 md:row-span-2' : '' %>">
  <a href="/blog/<%#= id %>" class="block">
    <div class="relative overflow-hidden">
      <img
        src="<%#= thumbnail %>"
        alt="<%#= title %>"
        class="w-full object-cover transition-transform duration-300 group-hover:scale-105 <%#= featured ? 'h-64 md:h-80' : 'h-48' %>"
      />
      <div class="absolute top-4 left-4">
        <span class="bg-blue-600 text-white px-3 py-1 rounded-full text-xs font-medium">
          <%#= category %>
        </span>
      </div>
    </div>

    <div class="p-6 <%#= featured ? 'md:p-8' : '' %>">
      <h2 class="font-bold text-gray-900 group-hover:text-blue-600 transition-colors mb-3 line-clamp-2 <%#= featured ? 'text-2xl md:text-3xl mb-4' : 'text-xl' %>">
        <%#= title %>
      </h2>

      <p class="text-gray-600 mb-4 line-clamp-3 <%#= featured ? 'text-lg leading-relaxed' : '' %>">
        <%#= excerpt %>
      </p>

      <div class="flex items-center justify-between text-sm text-gray-500">
        <div class="flex items-center space-x-4">
          <div class="flex items-center space-x-1">
            <!-- Replace with SVG icon -->
            <i class="lucide lucide-user" style="width: 14px; height: 14px;"></i>
            <span><%#= author %></span>
          </div>
          <div class="flex items-center space-x-1">
            <i class="lucide lucide-calendar" style="width: 14px; height: 14px;"></i>
            <span><%#= date %></span>
          </div>
        </div>
        <div class="flex items-center space-x-1">
          <i class="lucide lucide-clock" style="width: 14px; height: 14px;"></i>
          <span><%#= readTime %></span>
        </div>
      </div>
    </div>
  </a>
</article>

</body>
</html>